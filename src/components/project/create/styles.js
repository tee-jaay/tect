import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  formFieldsContainer: {
    borderRadius: "10px",
    padding: " 18px 12px",
  },
}));

export default useStyles;
