import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100%",
  },
  addTodo: {
    width: "100%",
    margin: "8px 0",
  },
}));
export default useStyles;
