import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100%",
  },
  heading: {
    paddingRight: "8px",
  },
  submitBtn: {},
}));

export default useStyles;
