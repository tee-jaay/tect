import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  footer: {
    marginTop: "auto",
    paddingTop: "20px",
  },
  copyright: {
    width: "100%",
    textAlign: "center",
    padding: "10px 0",
  },
}));

export default useStyles;
